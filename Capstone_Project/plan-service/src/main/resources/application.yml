server:
  port: 9003

spring:
  application:
    name: plan-service

  datasource:
    url: jdbc:postgresql://localhost:5432/capplandb
    username: postgres
    password: PVb@9284

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    non-secure-port: 9003
