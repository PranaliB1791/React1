server:
  port: 9004

spring:
  application:
    name: recharge-service

  datasource:
    url: jdbc:postgresql://localhost:5432/caprechargedb
    username: postgres
    password: PVb@9284

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          secret: your-256-bit-secret-key-your-256-bit-secret-key

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    non-secure-port: 9004
